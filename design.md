**敏捷周报管理系统**

# 一、系统整体架构

采用最简单的结构：

```
浏览器 (Vue)
        │
        │ HTTP API
        ▼
轻量后端 (Node.js)
        │
        ▼
数据库 (MongoDB)
```

特点：

* 前端：单页面应用
* 后端：简单REST API
* 数据库：1张表即可
* 无账号体系
* 用户名保存在 `localStorage`

---

# 二、数据模型设计

核心对象：**周报**

### 1 周报表 `weekly_reports`

| 字段         | 类型       | 说明              |
| ---------- | -------- | --------------- |
| id         | int      | 主键              |
| week       | varchar  | 周标识（如 2026-W14） |
| name       | varchar  | 姓名              |
| content    | json     | 工作内容            |
| created_at | datetime | 创建时间            |
| updated_at | datetime | 更新时间            |

### 2 工作内容 JSON 结构

```json
[
  {
    "task": "情报数据采集平台",
    "progress": "完成采集器架构设计"
  },
  {
    "task": "PoC自动生成系统",
    "progress": "完成模型训练方案设计"
  }
]
```

---

# 三、页面设计

系统只有 **两个模块**

```
+----------------------+
| 周报系统             |
+----------------------+
| 填写周报 | 查看周报 |
+----------------------+
```

---

# 四、填写周报页面

### 页面结构

```
姓名: [Bo Chen]

本周工作

+-------------------------------------+
| 任务/项目        | 工作内容/进展    |
+-------------------------------------+
| [输入框]         | [输入框]         |
| [输入框]         | [输入框]         |
+-------------------------------------+

[ + 添加工作项 ]

[ 提交周报 ]
```

---

### 功能

#### 1 自动填充姓名(基于localStorage)
#### 2 动态增加任务
#### 3 提交周报

# 五、查看周报页面

按照 **周维度展示**

```
2026-W14
-----------------------------------------------------

Bo Chen
1. 情报数据采集平台
   完成采集器架构设计

2. PoC自动生成系统
   完成模型训练方案设计

-----------------------------------------------------

Alice

1. 数据清洗模块
   完成规则设计

-----------------------------------------------------

[导出本周Word]
```

---


# 七、编辑规则

允许：

✔ 修改任务
✔ 修改进展

不允许：

✘ 删除整条周报

实现方式：

* 编辑保存
* 后端 **禁止删除接口**

---

# 八、Word导出

点击：

```
导出本周Word
```

API：

```
GET /api/export/2026-W14
```

返回：

```
weekly-report-2026-W14.docx
```

---

### Word模板结构

生成：

```
2026年第14周工作周报

Bo Chen

1. 情报数据采集平台
   完成采集器架构设计

2. PoC自动生成系统
   完成模型训练方案设计


Alice

1. 数据清洗模块
   完成规则设计
```

---

# 九、体验优化

### 自动识别当前周

JS：

```
2026-W14
```

算法：

```
ISO week
```

---

### 防止重复提交

同一人一周只允许一条：

```
unique (week,name)
```

---

### 自动保存草稿

```
localStorage
```

---
